/* -while loops are used when the number of 
iterations is not  known
-for loops are used when the number of iterations is known
-do while are used  when used like while loops but only difference is that 
the line statements run faster before checking the condition*/
#include<stdio.h>
#include<math.h>

int main(){
    /*for(int i= 0;i<5;i++){
        for(int j =0; j< 5; j++)
        printf("*");
    printf("\n");
    }*/
    /*for(int y = 0;y<10;y++){
        if (y == 4)
        {
            break;
        }
        printf("%d\n", y);
    }*/

    printf("%lf",pow(2,0));
    return 0;
}